# Project metadata
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Gradle daemon
org.gradle.daemon=true

# Kotlin compiler
kotlin.code.style=official

# JVM settings
systemProp.file.encoding=UTF-8
systemProp.user.timezone=Europe/Istanbul

# Build optimization
org.gradle.workers.max=4

# Maven publishing
mavenPublishUsername=
mavenPublishPassword=

# SonarQube
sonar.host.url=http://localhost:9000
sonar.login=

# Docker
docker.registry.url=
docker.registry.username=
docker.registry.password=

# Environment specific overrides
spring.profiles.active=dev

# Testing
junit.jupiter.execution.parallel.enabled=true
junit.jupiter.execution.parallel.mode.default=concurrent

# Gradle compatibility for different versions
org.gradle.java.installations.auto-download=false
org.gradle.java.installations.auto-detect=true

# Temporarily skip tests due to compilation issues
# skipTests=true  # Sprint 4: Re-enabling tests for completion